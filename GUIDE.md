# 소담 에이전틱 (SoDamAgentic) — 왕초보 가이드 (GUIDE)

> 🇰🇷 한국어 가이드 (기본) · 영어 가이드: [GUIDE.en.md](./GUIDE.en.md)
> 이 가이드는 **AI·컴퓨터·스마트폰·전자기기를 처음 다루는 분**도 그대로 따라 할 수 있게 만들었습니다.
> 짧은 버전이 필요하면 [README.md](./README.md)를 먼저 보세요. 이 문서는 **상세판**입니다.
> ⚠️ **현재 상태:** Phase 1 (MVP) 완료 + Phase 2 일부(F6 안전 기록·F7 Codex 안전 패리티) 완료. 일부는 아직 사람의 라이브 확인을 기다리고 있습니다(맨 끝 "현재 개발 상태" 참고).

---

## 목차

0. [이 도구가 무엇인가요](#what-is-this)
1. [사전 준비물](#prerequisites)
2. [필요 프로그램 다운로드·설치](#install-programs)
3. [빠른 시작 (3단계)](#quickstart)
4. [설치 방법 (상세)](#install-detail)
5. [실행·사용·작동 방법](#usage)
6. [명령어 목록](#commands)
7. [워크플로우 (작동 흐름)](#workflow)
8. [파일·문서 위치](#file-locations)
9. [아키텍처](#architecture)
10. [보안·데이터 흐름](#security-data-flow)
11. [제거 방법 (Uninstall)](#uninstall)
12. [문제·오류 대처](#troubleshooting)
13. [안전 주의사항](#safety-notes)
14. [라이선스·저작권·상업적 용도](#license-legal)
15. [업데이트 내용 요약](#changelog)
16. [FAQ (자주 묻는 질문)](#faq)
17. [현재 개발 상태 (정직하게)](#dev-status)

---

<a id="what-is-this"></a>
## 0. 이 도구가 무엇인가요? (한 문장)

> **소담**은 "AI에게 일을 제대로 시키는 법"을 **쉬운 한국어로 떠먹여 주는**, Claude Code/Codex용 **플러그인(추가 기능)**입니다.

비유: **AI는 공장의 기계, 당신은 그 공장을 설계하는 사람**입니다. "알아서 해"가 아니라 **무엇을·왜·어디까지** 만들지 먼저 정하고, AI가 한 일을 검토하고, 위험한 건 막습니다.

**해주는 4가지:**
| 기능 | 한 줄 설명 |
|---|---|
| 온보딩 | AI에게 일 시키는 4단계를 한국어로 안내 |
| 계획 먼저 | 코드 짜기 전에 "무엇을·왜·완성기준" 계획을 먼저 보여주고 승인받음 |
| 변경점 검토 | 바꾼 내용을 "무엇을 / 왜 / 위험은?"으로 쉽게 요약 |
| 안전 | 위험 명령·비밀번호 노출·엉뚱한 위치 쓰기·설정 변경을 자동으로 막거나 확인 |

> 💬 **용어가 낯설다면:** `플러그인`=AI 프로그램에 기능을 더하는 부품 · `훅(hook)`=AI가 뭘 하기 직전에 자동으로 끼어드는 감시장치 · `마켓플레이스`=플러그인을 받아오는 "가게"(여기선 GitHub 주소) · `슬래시 명령`=`/`로 시작하는 명령 · `세션`=지금 이 대화창 하나(닫으면 끝) · `MCP`=AI가 외부 도구와 대화하는 규격(이 플러그인은 사용하지 않음).

---

<a id="prerequisites"></a>
## 1. 사전 준비물 (이게 있어야 작동합니다)

| 준비물 | 왜 필요한가 | 어디서 받나 |
|---|---|---|
| **Node.js 18 이상** | 안전장치(훅)가 이걸로 돌아갑니다. 없으면 안전 기능이 안 켜져요 | https://nodejs.org → "LTS" 버전 다운로드 |
| **Claude Code** | 소담이 설치되는 프로그램 (또는 Codex) | https://code.claude.com (공식 안내 따라 설치) |
| **GitHub 계정 + 접근 권한** | 소담은 GitHub(인터넷 코드 창고)에서 받습니다. 현재 **비공개**라 권한 필요 | https://github.com (가입 무료) |
| **인터넷 연결** | 설치할 때 필요 | — |

> 💡 **확인 방법(선택):** 검은 창(터미널)에 `node -v` 입력 → `v18....` 이상이면 OK.

---

<a id="install-programs"></a>
## 2. 필요 프로그램 다운로드·설치 (단계별)

### 2-1. Node.js 설치
1. https://nodejs.org 접속 → 초록색 **"LTS"** 버튼 클릭해 내려받기.
2. 받은 설치 파일을 더블클릭 → "다음 → 다음 → 설치" (기본값 그대로).
3. 설치 후 컴퓨터를 한 번 껐다 켜면 확실합니다.

### 2-2. Claude Code 준비
- 이미 Claude Code를 쓰고 계신다면 건너뛰세요. (지금 이 대화가 Claude Code입니다.)
- 처음이라면 공식 안내(https://code.claude.com)를 따라 설치하세요. (Node.js가 먼저 깔려 있어야 합니다.)

---

<a id="quickstart"></a>
## 3. 빠른 시작 (3단계 골든패스)

1. **설치:** Claude Code 입력칸에
   ```
   /plugin marketplace add https://github.com/sodam-ai/SoDam-Agentic-Eng
   /plugin install sodam-agentic@sodam
   ```
2. **시작:** `/sodam-agentic-start` → 한국어 안내가 뜸.
3. **시키기:** "○○ 만들어줘"라고 부탁 → **계획**이 먼저 뜨면 "네" → 작업 → **검토 요약** 확인.

→ 여기까지 5분이면 "됐다" 경험 완료.

---

<a id="install-detail"></a>
## 4. 설치 방법 (상세)

> ⚠️ 현재 저장소는 **비공개(private)**입니다. 본인 GitHub 계정에 **접근 권한이 있을 때만** 설치됩니다.

**Claude Code:**
1. 마켓플레이스(가게) 등록 — 입력칸에 그대로 붙여넣고 Enter:
   ```
   /plugin marketplace add https://github.com/sodam-ai/SoDam-Agentic-Eng
   ```
   → "추가됨 / sodam" 비슷한 메시지가 보이면 성공.
   > 🖼️ *(스크린샷 자리 — 실제 화면 캡처는 사람이 라이브 테스트 시 추가 예정)*
2. 설치:
   ```
   /plugin install sodam-agentic@sodam
   ```
   → "installed / 설치됨" 이 보이면 성공.
3. 확인: `/sodam-agentic` 까지 입력 → 명령 4개(`-start`, `-plan`, `-review`, `-log`)가 뜨면 완료.
   > 🖼️ *(스크린샷 자리)*

**(개발자 본인 로컬 테스트용)** 인터넷 대신 내 컴퓨터 폴더로도 설치 가능:
```
/plugin marketplace add D:/AI_Dev_Work/2026y/26y_06m_26d_SoDam-Agentic-Eng
/plugin install sodam-agentic@sodam
```

**Codex 사용자:**
1. 저장소를 클론: `git clone https://github.com/sodam-ai/SoDam-Agentic-Eng`
2. 내 프로젝트 폴더에서: `node C:\경로\SoDam-Agentic-Eng\codex\install.mjs`
3. 완료: 스킬이 내 프로젝트의 `.agents/skills/`에 복사됨.
> ⚠️ Codex에도 **같은 안전장치(F4·F6)가 등록됩니다**(F7, 2026-07-15). 다만 Codex에서 "확인(ask)" 창이 실제로 뜨는지·설정 로드 위치가 정확한지는 아직 사람이 실제 Codex로 직접 확인하지 않았습니다 — [§10 보안·데이터 흐름](#security-data-flow) 참고.

---

<a id="usage"></a>
## 5. 실행·사용·작동 방법 (단계별)

1. **`/sodam-agentic-start`** 입력 → "AI에게 일 시키는 4단계" 온보딩을 읽습니다.
2. (권장) **`/init`** 을 한 번 실행 → AI가 현재 폴더(프로젝트)를 인식합니다. *초보자가 가장 잘 건너뜁니다.*
3. **자연어로 시키기:** 예) "메모장 웹페이지 만들어줘". (확인·진단 명령부터 앞세울 필요 없습니다 — 그냥 시키는 게 먼저입니다.)
4. **계획 확인:** AI가 코드 전에 "①무엇을 ②왜 ③완성기준" 계획을 보여주면, 읽고 **"네/진행"** 으로 승인.
5. **검토:** 작업이 끝나면 "무엇을·왜 바꿨고 위험은?" 요약을 읽고 판단.
6. **안전:** 위험한 명령(폴더 통째 삭제 등)을 시키면 자동으로 막거나 "확인할까요?" 묻습니다.

---

<a id="commands"></a>
## 6. 명령어 목록

| 명령어 | 언제 쓰나 |
|---|---|
| `/sodam-agentic-start` | 처음 시작·온보딩 안내 |
| `/sodam-agentic-plan` | "계획 먼저" 기능 (보통 자동 발동) |
| `/sodam-agentic-review` | "변경점 검토" 기능 (보통 자동 발동) |
| `/sodam-agentic-log` | 막히거나(deny) 확인받은(ask) 기록 조회 (F6, 2026-07-15 추가) |

> 명령은 `/플러그인이름:명령` 형식(`/sodam-agentic:sodam-agentic-start`)으로도 작동합니다.

---

<a id="workflow"></a>
## 7. 워크플로우 (작동 흐름)

```
[시작] /sodam-agentic-start  →  안전 켜짐 + 4단계 안내
   │
   ▼
[계획 먼저]  "만들어줘"  →  AI가 계획 제시  →  당신이 승인
   │
   ▼
[실행]  AI 작업  ──(위험 시)──▶  [안전]  자동 차단 / 확인
   │
   ▼
[검토]  무엇을·왜·위험  요약  →  당신이 판단  →  완료
```
핵심 원칙: **"AI가 다 알아서"가 아니라, 사람이 운전석.**

---

<a id="file-locations"></a>
## 8. 파일·문서 위치

**개발 폴더(원본):** `D:\AI_Dev_Work\2026y\26y_06m_26d_SoDam-Agentic-Eng`
**GitHub(인터넷):** https://github.com/sodam-ai/SoDam-Agentic-Eng (비공개, 브랜치 `init-mvp`)

| 무엇 | 위치 |
|---|---|
| 플러그인 설명 파일 | `.claude-plugin/plugin.json`, `.claude-plugin/marketplace.json` |
| 온보딩 명령 | `commands/sodam-agentic-start.md` |
| 안전 기록 조회 명령 | `commands/sodam-agentic-log.md` (F6) |
| 계획·검토 기능 | `skills/sodam-agentic-plan/`, `skills/sodam-agentic-review/` |
| 검토 보조 AI | `agents/easy-reviewer.md` |
| 안전장치(훅) | `hooks/hooks.json`, `hooks/guard.mjs`, `hooks/delegate.mjs` |
| 안전 규칙(데이터) | `data/agentic-rules.json` |
| 구조 점검 도구 | `scripts/validate.mjs` |
| Codex 설치기 | `codex/install.mjs` (F5·F7 겸용) |
| **안전 기록 파일(내 컴퓨터, F6)** | `~/.sodamagentic/safety-log.jsonl` (플러그인 폴더 밖, 사용자 홈 폴더) |
| **Codex 훅 등록 파일(F7)** | 내 프로젝트의 `.codex/hooks.json` (설치 시 자동 생성·병합) |
| 문서(루트) | `README.md`/`README.en.md`(요약) · `GUIDE.md`/`GUIDE.en.md`(이 문서) · 각 `.html` 버전 |
| 법률 문서 | `LICENSE`(Apache-2.0 전문) · `NOTICE`(저작권 고지) |
| AI 공유 지침 | `AGENTS.md`(Claude Code·Codex 공용) · `CLAUDE.md`(포인터) |
| 6형제 협업 문서 | `docs/family-synergy.md` |
| 변경 이력 | `CHANGELOG.md` |
| 설치 후 위치 | `C:\Users\<사용자>\AppData\Roaming\claude-code\plugins\` (자동) |

---

<a id="architecture"></a>
## 9. 아키텍처

이 플러그인은 서버·DB·로그인이 **없는, 순수 로컬 도구**입니다. 구성요소는 이렇습니다:

| 구성요소 | 파일 | 역할 |
|---|---|---|
| 매니페스트 | `.claude-plugin/plugin.json`, `marketplace.json` | Claude Code에 "이 폴더가 플러그인"이라고 알려주는 설명서 |
| 온보딩 (F1) | `commands/sodam-agentic-start.md` | `/sodam-agentic-start` 슬래시 명령 정의 |
| 계획·검토 (F2·F3) | `skills/sodam-agentic-plan/`, `skills/sodam-agentic-review/` | 새 작업 요청·변경 완료 시 자동 발동하는 안내 규칙 |
| 검토 보조 에이전트 | `agents/easy-reviewer.md` | 변경이 많을 때 F3가 위임하는 **읽기 전용** 서브 AI |
| 안전 훅 (F4) | `hooks/hooks.json`(배선표) + `hooks/guard.mjs`(판정 로직) + `hooks/delegate.mjs`(형제 감지) | Bash·PowerShell·Write·Edit 실행 **직전**에 항상 개입 |
| 안전 규칙 데이터 | `data/agentic-rules.json` | 코드 수정 없이 조정 가능한 규칙 값 |
| 안전 기록 (F6) | `hooks/guard.mjs`의 `decide()` 내부 로직 + `commands/sodam-agentic-log.md` | 확인(ask)·차단(deny) 판정마다 `~/.sodamagentic/safety-log.jsonl`에 기록, 명령으로 조회 |
| Codex 지원 (F5) | `codex/install.mjs` | 마켓플레이스가 없는 Codex용 별도 설치기(스킬을 파일로 복사) |
| Codex 안전 패리티 (F7) | `codex/install.mjs`가 `.codex/hooks.json`에 등록 | 새 안전 로직을 따로 만들지 않고 **같은 `hooks/guard.mjs`를 Codex에도 그대로 등록**(기존 설정 있으면 병합) |

### 안전 훅(F4) 판정 흐름

```
Claude Code가 도구(Bash·Write·Edit 등)를 실행하려는 순간
        │
        ▼
   hooks/guard.mjs 가 먼저 판정 (PreToolUse = "실행 직전")
        │
        ├─ 안전(safe)          → 그냥 통과
        ├─ 확인 필요(ask)       → "정말 진행할까요?" 질문
        ├─ 위험(deny)          → "막았어요" (형제 SoDamHarness가 있으면 겹치는 항목은 위임)
        └─ 치명(catastrophic)  → "막았어요" (형제 유무와 **무관하게 항상** 차단)
```

- `hooks/delegate.mjs`의 `isHarnessAlive()`가 형제 플러그인 **SoDamHarness**의 존재를 확인합니다.
- Harness가 있으면 "겹치는" 위험(재귀 삭제·민감 경로 등)은 Harness에 넘겨 확인창이 두 번 뜨지 않게 합니다(Harness는 자동 백업·되돌리기까지 더해줍니다).
- **단, 되돌릴 수 없는 치명 명령(디스크 초기화급)은 Harness 설치 여부와 무관하게 이 플러그인도 항상 자체적으로 막습니다** — 형제가 없거나 고장 났을 때를 대비한 이중 안전장치입니다.
- **확인(ask)·차단(deny) 판정은 F6에 의해 `~/.sodamagentic/safety-log.jsonl`에 자동 기록됩니다**(안전하게 통과한 작업은 기록 안 함, 비밀값은 저장 전 자동으로 가려짐). 조회는 [`/sodam-agentic-log`](#commands).

---

<a id="security-data-flow"></a>
## 10. 보안·데이터 흐름

- **이 플러그인이 읽는 것:** Claude Code가 실행하려는 도구 이름과 인자(예: 실행할 명령어 문자열, 쓰려는 파일 경로)뿐입니다. 이 정보는 표준입력(stdin)으로 훅에 전달됩니다. 그 외 파일 내용이나 대화 내용을 따로 들여다보지 않습니다.
- **이 플러그인이 절대 하지 않는 것:** 자체 네트워크 요청(외부 서버로 전송) · API 키·비밀번호·토큰 저장 또는 기록 · 외부 코드 자동 실행. (자기보안 자가검증 결과 0건 — `hooks/_selftest.mjs`)
- **영구 데이터(2026-07-15 갱신 — F6 반영):** 안전 훅이 **확인(ask)·차단(deny)** 판정을 내릴 때만 `~/.sodamagentic/safety-log.jsonl`(내 컴퓨터 사용자 폴더, 플러그인 개발 폴더 밖)에 한 줄씩 기록을 남깁니다. 기록 항목은 `{판정, 대상, 이유, 시각}`이며, 대상 문자열에 비밀값 패턴(API 키 등)이 있으면 저장 전 자동으로 `[REDACTED]`로 가립니다. **안전하게 통과한 작업은 기록하지 않습니다**(로그가 무한정 커지는 것 방지). 이 기록은 외부로 전송되지 않고 내 컴퓨터에만 남으며, 기록 자체가 실패해도(예: 디스크 문제) 안전 판정(차단/확인)에는 영향을 주지 않습니다. 조회는 [`/sodam-agentic-log`](#commands) 명령입니다.

### 실제 판정 3단계 예시

| 단계 | 예시 상황 | 실제로 뜨는 안내(요약) |
|---|---|---|
| ✅ 안전(통과) | `echo hello`, 새 파일 만들기 | (아무 메시지 없이 그냥 진행) |
| ❓ 확인(ask) | 파일 하나 삭제, `.claude/settings.json` 수정, 작업폴더 밖 쓰기 | "[소담 에이전틱] 되돌리기 어려운 작업이에요… 정말 진행할까요?" |
| ⛔ 차단(deny) | API 키 노출 시도 | "[소담 에이전틱] API 키·비밀값이 노출될 수 있는 작업이라 막았어요…" |
| ⛔ 차단(deny) | 폴더 통째 삭제 | "[소담 에이전틱] 폴더를 통째로 지우는 작업은 안전하게 막았어요…" |
| ⛔ 차단(항상, 치명) | 디스크 초기화급 명령, `curl 주소 \| bash`(인터넷 코드 확인 없이 바로 실행) | "[소담 에이전틱] 되돌릴 수 없는 위험한 명령이라 막았어요…" |

> 이 표는 이 저장소의 실제 코드(`hooks/guard.mjs`)에 있는 문구를 그대로 옮긴 것입니다 — 과장 없이, 실제 동작 그대로입니다. **`[소담 에이전틱]` 접두사(2026-07-16 추가)**는 SoDamHarness 등 형제 플러그인과 동시 설치돼 훅이 함께 발동해도 어느 플러그인이 막았는지 바로 구분되도록 붙인 출처 표시입니다.

> **일관성 보강(2026-07-12):** "AI에게 직접 명령어로 시켜서" 같은 방식으로도 위 판정을 피해 갈 수 없습니다 — 자연어로 부탁하든, 명령어를 직접 지정하든 동일한 기준으로 검사합니다.

> **기록 보강(2026-07-15, F6):** 위 표의 ❓확인·⛔차단 항목은 이제 자동으로 안전 기록(`~/.sodamagentic/safety-log.jsonl`)에 남습니다. ✅안전(통과)은 기록되지 않습니다.

---

<a id="uninstall"></a>
## 11. 제거 방법 (Uninstall)

**Claude Code:**
1. 입력칸에 `/plugin uninstall sodam-agentic` (또는 마켓플레이스 화면에서 제거 선택).
2. 확인: `/sodam-agentic` 를 쳐도 명령이 더는 안 뜨면 제거 완료.

**Codex:**
- 설치가 "파일 복사" 방식이므로, 내 프로젝트의 `.agents/skills/` 폴더에서 소담 관련 스킬 폴더를 직접 삭제하면 됩니다.

**남는 데이터(2026-07-15 갱신 — F6 반영):** 플러그인을 제거해도 안전 기록 파일 `~/.sodamagentic/safety-log.jsonl`은 **자동으로 지워지지 않습니다**(플러그인 폴더 밖, 사용자 홈 폴더에 저장되기 때문입니다). 완전히 지우고 싶으면 이 파일을 직접 삭제하세요. Codex를 함께 썼다면 프로젝트의 `.codex/hooks.json`에 등록된 항목(F7)도 제거 후 그대로 남으므로, 지우려면 해당 JSON의 `PreToolUse` 항목을 직접 삭제하면 됩니다.

---

<a id="troubleshooting"></a>
## 12. 문제·오류 대처

| 증상 (이렇게 보임) | 왜 (원인) | 이렇게 (해결) |
|---|---|---|
| 깔았는데 아무 일도 안 일어남 | `/init` 안 함 / 온보딩 안 읽음 | `/sodam-agentic-start` 로 상태부터 확인 |
| `/sodam-agentic` 쳐도 안 뜸 | 설치 안 됨 | `/plugin install sodam-agentic@sodam` 다시 |
| 설치할 때 "권한 없음/접근 불가" | 비공개 저장소 권한 없음 | 본인 GitHub 계정 로그인·접근 권한 확인 |
| "Node가 없다"고 나옴 | Node.js 미설치 | [§2](#install-programs) 따라 Node.js 18+ 설치 후 재시도 |
| 한글이 `□□□`로 깨짐 | 글자 표시 문제 | 화면 캡처해서 문의 (영문 버전 안내 가능) |
| 계획 없이 바로 코드부터 짬 | 다른 스킬에 밀림(F2는 "부탁"이라 강제 아님) | 정상일 수 있음 — 알려주시면 보강(Phase 2에서 강제화 예정) |
| 위험 명령이 안 막힘 | 안전 훅 미작동 | 캡처해서 문의 (Node·설치 상태 점검) |
| 확인창이 너무 자주 뜸 | 여러 안전장치가 겹침 | 정말 위험한 것만 뜨도록 조정 가능 — 알려주시면 조정 |
| 명령어가 옛 이름으로 보임 | 설치본 캐시 | `/plugin marketplace update sodam` → 재설치 |
| 비밀번호·API 키를 넣어도 되나요? | — | **절대 넣지 마세요.** 본인 환경(`.env` 등)에만 보관 |
| 지우고 싶어요 / 업데이트 후 이상해요 | 제거·업데이트 | [§11 제거 방법](#uninstall) 참고 |
| 지난번에 뭐가 막혔는지 기억이 안 남 | — | `/sodam-agentic-log` 명령으로 조회(막힘·확인만 기록, 안전하게 통과한 작업은 기록 안 됨) |
| Codex에서 확인(ask) 창이 안 뜨는 것 같음 | 아직 라이브로 확인 안 된 부분(F7, 2026-07-15) | Codex 자체의 `/hooks` 명령으로 등록 여부를 먼저 확인 후 알려주세요 |

---

<a id="safety-notes"></a>
## 13. 안전 주의사항 (꼭 지키기)

- **비밀번호·API 키·`.env` 같은 비밀정보를 코드·문서·채팅 어디에도 넣지 마세요.**
- 안전장치는 **"되돌릴 수 없는 위험은 막고, 나머지는 확인받는"** 방식입니다. **"100% 안전"이 아닙니다** — 마지막 판단은 사람이 합니다.
- 새로 받은 외부 파일·도구를 AI가 자동 실행하지 않게 주의하세요.
- 화면 아래 **자동 승인(auto-accept/bypass permissions)** 모드가 켜져 있으면 확인창이 조용히 통과됩니다 — 안전하게 쓰려면 `Shift+Tab`으로 "매번 물어봄" 모드를 권장합니다.

---

<a id="license-legal"></a>
## 14. 라이선스 · 저작권 · 상업적 용도 (중요 — 꼭 읽어주세요)

> ⚠️ **이 문서는 법률 자문이 아닙니다.** 아래는 일반 안내이며, 실제 배포·상업 사용 전에는 **본인 책임으로** 확인하시기 바랍니다.

- **현재 배포 상태(중요):** 이 저장소는 **비공개(private, PRIVATE)**이며 아직 일반 대중에게 공개 배포되지 않았습니다(개발자 본인의 개인용 도구로 사용 중, 2026-07-15 확정). 아래 라이선스 조건은 **향후 공개·배포하는 경우를 대비해 미리 명시**해 둔 것입니다.
- **라이선스(확정):** **Apache License 2.0** · 저작권자 **SoDam AI Studio** · 2026. 전문은 저장소 루트 `LICENSE` 파일, 고지는 `NOTICE` 파일에 있습니다.
- **허용 범위(NOTICE 보존 조건):** 수정 ✅ / 복제 ✅ / 재배포 ✅ / 상업적 사용 ✅ / 판매 ✅ / 서비스 운영 ✅ / 교육 ✅ / 회사·고객사 납품 ✅.
- **파생물 표시 의무(Apache-2.0 §4 요건):** 이 코드를 수정해서 재배포할 경우, **수정한 파일에 "원본에서 변경했다"는 표시**를 남겨야 하고, 원본 `LICENSE`·`NOTICE` 사본을 함께 배포해야 합니다.
- **보증·책임 제한(Apache-2.0 §7·§8 요건):** 이 소프트웨어는 **"있는 그대로(AS IS)" 제공**되며 **어떠한 보증도 하지 않습니다.** 저작권자·기여자는 이 소프트웨어 사용으로 발생하는 어떠한 직접·간접 손해(데이터 손실·영업 손실·업무 중단 등 포함)에도 **법이 허용하는 한도 내에서 책임지지 않습니다.** 사용 결과는 **사용자 책임**입니다.
- **AI 모델 사용료·약관은 별도:** 이 키트는 **무료(Apache-2.0)**지만, **AI 모델(Claude/Codex)의 사용료·약관은 Anthropic·OpenAI를 따릅니다.** 이 도구의 라이선스와는 무관합니다.
- **타사 상표:** "Claude·Anthropic·Codex·OpenAI"는 각사의 상표입니다. **설명적(호환·대상) 표기로만** 사용하며, **공식 제휴·보증으로 오인하게 하지 않습니다.**
- **무단 포함 금지:** 타인의 저작물·상표·로고·개인정보·고객사 정보·비밀정보는 포함하지 않습니다(이 저장소에도 0 — `_selftest.mjs` 자가검증으로 반복 확인됨).
- **외부 자료 추가 시:** 폰트·이미지·아이콘 등을 추가하면 **각 자료의 상업 이용 가능 라이선스를 별도 확인**하세요.
- **GPL/AGPL 등 강한 카피레프트 코드 차용은 지양**합니다(상업·납품 시 소스 공개 의무 전파 위험).
- **재배포·공개 전 최소 체크리스트:** ① `LICENSE` 전문 포함 ② `NOTICE` 파일 포함(저작권 고지 보존) ③ 수정한 파일에 변경 사실 표시 ④ "Claude/Codex/Anthropic/OpenAI" 등 상표를 보증·제휴로 오인시키는 방식으로 쓰지 않기 ⑤ 아래 "아직 확인 중인 것" 항목을 먼저 해소.
- ⚠️ **아직 확인 중인 것(법무 검토 대기, 공개 배포 전까지는 필수 아님):** 제품명·"에이전틱 엔지니어링" 표현의 상표 충돌 여부, 참고한 외부 코드의 정확한 라이선스 재확인. 확정되는 대로 이 절을 갱신합니다.

---

<a id="changelog"></a>
## 15. 업데이트 내용 요약

<details>
<summary><b>📦 [0.1.0] — 2026-06-28 (최초 배포, Phase 1 MVP)</b> — 클릭하면 펼쳐집니다</summary>

**추가된 기능**
- F1 온보딩 — 한국어 4단계 안내 (Codex 공유 포함)
- F2 계획 먼저 — 코드 전 "무엇을·왜·완성기준" 승인
- F3 변경점 검토 — 쉬운 말 요약 + 위임 에이전트
- F4 안전 훅 — 최소 폴백 4종 차단, Harness 위임, fail-closed
- F5 두 도구(Claude Code + Codex) 설치 지원

**추가된 문서·검증 도구**
- README(한/영)·가이드(한/영)·6형제 협업 문서(`family-synergy.md`)
- 구조 검증기(`validate.mjs`)·6형제 헬스체크·안전훅 자가검증(`_selftest.mjs`)
- `LICENSE`(Apache-2.0)·`NOTICE`

**알려진 한계**
- F2/F3는 "부탁"이라 강제 안 됨(다른 스킬에 밀릴 수 있음) — Phase 2에서 강제화 예정
- Codex에서는 F4 안전이 Claude Code보다 약함 — Phase 2에서 동등화 예정

</details>

<details>
<summary><b>🔧 2026-07-07 안전 강화 (같은 버전 내 패치)</b> — 클릭하면 펼쳐집니다</summary>

- 안전 폴백(F4)이 특정 조건에서 잠들어 있을 수 있던 결함을 찾아 수정 — 이제 설치되면 항상 켜집니다.
- 되돌릴 수 없는 치명 명령은 형제 플러그인(SoDamHarness) 설치 여부와 무관하게 항상 차단하도록 강화(이중 안전장치).
- 자동 승인 모드에서는 확인창이 조용히 통과된다는 한계를 온보딩 안내에 명시.
- 이 문서(README·GUIDE) 전면 개정 — 오래된 안내(라이선스 미확정 등) 수정, 아키텍처·보안·FAQ·제거방법 절 추가.

</details>

<details>
<summary><b>✅ 2026-07-11 라이브 검증 + 버그 수정 (같은 버전 내 패치)</b> — 클릭하면 펼쳐집니다</summary>

- **안전 훅(F4)이 실제 사용 화면에서 진짜로 작동함을 확인했습니다.** 지금까지는 자체 검사(코드 시뮬레이션)만 통과한 상태였는데, 이번에 실제로 위험해 보이는 명령을 시도해 정말로 막히는지 눈으로 확인했습니다.
- 그 과정에서 실제 문제 하나를 발견해 바로 고쳤습니다: 여러 작업을 한 번에 이어서 시키면(예: "폴더 이동하고 파일 만들어줘"), 전혀 위험하지 않은 평범한 작업까지 "위험하다"며 잘못 막는 경우가 있었습니다. 지금은 고쳐졌고, 고친 뒤에도 진짜 위험한 명령은 여전히 잘 막힌다는 것까지 다시 확인했습니다.
- Codex 설치 안내 문구 중 일부가 서로 다르게 적혀 있던 것(설치 시작 안내와 완료 안내가 다른 말을 하던 것)을 통일했습니다.
- ⚠️ **설치돼 있는 버전이 오래됐다면 이 수정이 아직 반영 안 됐을 수 있습니다.** 한 번 지웠다가 다시 설치하시면 최신 내용이 적용됩니다([§12 문제·오류 대처](#troubleshooting) 참고).

</details>

<details>
<summary><b>🛡️ 2026-07-12·13 안전 강화 — 실제 버그 5건 발견·수정 + 보안감사 완료</b> — 클릭하면 펼쳐집니다</summary>

**실사용 검증(라이브 테스트)으로 찾아 고친 것**
- 안전장치에 아주 드문 입력값(예: 잘못된 형식의 신호)이 들어오면 프로그램이 멈추던 문제를 찾아 고쳤습니다.
- **"작업폴더 밖에 쓰지 못하게 막는" 규칙이 일부 상황에서 빠져있던 것**을 찾아 보강했습니다.
- 바로가기(심볼릭 링크)를 새로 만드는 명령 자체를 확인받도록 추가했습니다.
- **(가장 중요) "AI에게 명령어로 직접" 시키는 경우, 위 작업폴더 규칙이 통째로 건너뛰어지던 심각한 구멍을 찾아 막았습니다.** 자연어로 부탁하든 명령어로 직접 시키든 이제 똑같이 검사됩니다.
- **인터넷에서 받은 코드를 확인 없이 바로 실행하는 것**(`curl 주소 | bash` 같은 흔한 해킹 수법)을 막는 안전장치를 새로 추가했습니다 — 지금까지 이 부분은 아예 검사하지 않고 있었습니다.
- 변경점 검토(F3) 기능이 더 잘 뜨도록 문구를 보강했습니다. **다만 효과가 확실히 좋아졌는지는 아직 확인 중입니다** — 과장하지 않고 정직하게 남깁니다.

**보안 점검(OWASP 기준) 별도 수행**
- 비밀정보 노출·설정 파일 보호·오류 메시지 정보노출·의존성 등 국제 보안 점검 기준(OWASP ASVS)의 필수 항목을 전부 다시 확인했고, 통과했습니다.
- 저장소에 실수로 남은 시크릿(비밀 값)이 있는지 전체 파일을 다시 스캔 — 0건.

**회귀 테스트**: 안전장치 자체 검사 22건 → **42건**으로 늘렸고, 전부 통과 확인.

</details>

<details>
<summary><b>✅ 2026-07-15 — F6 안전 기록 + F7 Codex 안전 패리티 구현 완료</b> — 클릭하면 펼쳐집니다</summary>

**F6. 안전 기록**
- 안전장치가 확인(ask)·차단(deny) 판정을 내릴 때마다 내 컴퓨터의 `~/.sodamagentic/safety-log.jsonl`에 기록을 남기도록 했습니다(안전하게 통과한 작업은 기록 안 함).
- 기록에 비밀값(API 키 등)이 그대로 남지 않도록 자동으로 가립니다(`[REDACTED]`).
- 새 명령 **`/sodam-agentic-log`**로 "최근에 무엇이 막혔는지·확인받았는지"를 쉬운 한국어로 조회할 수 있습니다.
- 기록이 실패해도(예: 디스크 문제) 안전 판정 자체는 절대 영향받지 않도록 만들었습니다.

**F7. Codex 안전 패리티**
- Codex의 안전 훅 방식이 Claude Code와 사실상 같다는 것을 실제 Codex 공식 문서로 확인했습니다.
- 그래서 새 안전 로직을 따로 만들지 않고, Claude Code에서 쓰던 **같은 안전장치(`hooks/guard.mjs`)를 Codex에도 그대로 등록**하도록 설치 과정을 확장했습니다(`.codex/hooks.json`에 등록, 기존 설정이 있으면 덮어쓰지 않고 합침).
- ⚠️ **정직한 한계:** Codex에서 "확인(ask)" 창이 실제로 화면에 뜨는지, 설정 파일을 Codex가 정확히 이 위치에서 읽는지는 아직 사람이 실제 Codex로 직접 써보며 확인하지 않았습니다. 등록까지는 자동 검사로 확인했지만, 마지막 확인은 사람 몫으로 남아 있습니다.

**검증**: 안전장치 자체 검사 42건 → **54건**으로 늘렸고, 전부 통과 확인. 구조 검증도 이상 없음.

</details>

<details>
<summary><b>🗓️ 여전히 예정된 것 (착수 여부 미결정)</b> — 클릭하면 펼쳐집니다</summary>

- F2/F3를 스킬이 아닌 강제 훅으로 승격(자동 발동 보장) — 아직 착수 여부가 결정되지 않았습니다.

</details>

---

<a id="faq"></a>
## 16. FAQ (자주 묻는 질문)

**Q. 이거 정말 안전한가요?**
A. 아니요, "100% 안전"은 아닙니다. "되돌릴 수 없는 위험은 막고, 나머지는 확인받는" 정도입니다. 최종 판단은 항상 사람이 합니다.

**Q. 돈이 드나요?**
A. 이 플러그인 자체는 무료(Apache-2.0)입니다. 다만 Claude/Codex를 쓰는 비용(모델 사용료)은 Anthropic·OpenAI 약관을 따로 따릅니다.

**Q. Codex에서도 Claude Code와 똑같이 안전한가요?**
A. 이제 **같은 안전장치가 Codex에도 등록됩니다**(F7, 2026-07-15). 계획(F2)·검토(F3)·차단·안전 기록(F6)까지 같은 로직을 씁니다. 다만 **Codex에서 "확인해도 될까요?" 창이 실제로 뜨는지, 설정 로드 위치가 정확한지는 아직 사람이 직접 확인하지 않았습니다** — 100% 동등하다고 단정하지 않고 정직하게 남겨둡니다.

**Q. 막히거나 확인받은 기록을 나중에 다시 볼 수 있나요?**
A. 됩니다. `/sodam-agentic-log` 명령으로 최근 기록을 쉬운 한국어로 볼 수 있습니다. 단, **안전하게 통과한 작업은 기록되지 않고, 막히거나(deny) 확인받은(ask) 것만** 남습니다. 기록은 내 컴퓨터(`~/.sodamagentic/safety-log.jsonl`)에만 저장되고 어디로도 전송되지 않습니다.

**Q. SoDamHarness 없이 써도 되나요?**
A. 됩니다. Harness가 없으면 이 플러그인의 "최소 안전 폴백"이 전체 모드로 작동합니다. 다만 자동 백업·되돌리기 같은 더 강한 기능은 Harness가 있어야 합니다.

**Q. 상업적으로 써도 되나요?**
A. 이 플러그인 자체는 Apache-2.0이라 상업 사용·재배포·서비스 운영 모두 가능합니다(NOTICE 파일 보존 조건). 단 Claude/Codex 모델 사용의 상업 조건은 각 제공사 약관을 확인하세요.

**Q. 다른 5개 형제 플러그인도 꼭 설치해야 하나요?**
A. 아니요. 이 플러그인 혼자서도 최소 안전으로 작동합니다. 다만 SoDamHarness와 같이 쓰면 더 강력해집니다(README의 "소담 패밀리" 표 참고).

**Q. 제가 시킨 작업 내용이 어딘가로 전송되나요?**
A. 이 플러그인 자체는 네트워크 요청을 보내지 않습니다([§10 보안·데이터 흐름](#security-data-flow) 참고). Claude Code/Codex 자체가 AI 모델과 통신하는 것과는 별개입니다. (안전 기록(F6)은 전송이 아니라 **내 컴퓨터 파일에 저장**하는 것입니다 — 바로 위 질문 참고.)

---

<a id="dev-status"></a>
## 17. 현재 개발 상태 (정직하게)

- ✅ **검증됨:** 설치·한글 표시·명령 일관성·**온보딩(F1) 실제 작동**.
- ✅ **코드 완료 + 자가검증 통과:** 안전 훅(F4)이 항상 켜지도록 수정, 되돌릴 수 없는 치명 명령은 형제 유무와 무관하게 항상 차단, 자동 승인 모드 한계를 온보딩에 명시. **안전 기록(F6)·Codex 안전 패리티(F7) 구현 완료(2026-07-15).** 자체 자가검증 54개 테스트 전부 통과.
- ✅ **라이브 검증 여러 차례 진행(2026-07-11~13):** 실제 사용 화면에서 반복 테스트하며 실제 버그 5건을 찾아 고쳤고(위 "업데이트 내용 요약" 참고), 국제 보안 점검 기준(OWASP)의 필수 항목도 전부 재확인했습니다.
- ⚠️ **아직 확실하지 않은 것 — 정직하게 밝힙니다:** 변경점 검토(F3)가 "완전히 각본 없는" 실제 사용 상황에서도 항상 자동으로 뜨는지는 여러 번 테스트해도 아직 결론이 안 났습니다. Codex에서 F7(확인창·기록)이 실제로 작동하는지도 아직 사람이 라이브로 확인하지 않았습니다.
- ⬜ **아직 사람이 직접 확인해야 하는 것:** ① Codex에서 F7(안전 확인창·설정 로드 경로)이 실제로 작동하는지 라이브 확인 ② (공개 배포를 고려할 때만 필요) 비개발자 베타 테스트·법무 최종 확인.
- **개인용 확정(2026-07-15):** 이 도구는 개발자 본인이 직접 실제 사용하려고 만든 개인용 도구입니다. 위 ②(베타·법무)는 **지금 당장은 필수 전제조건이 아니며**, 추후 공개 공유를 고려할 때 다시 필요해집니다. 거친 부분이 있을 수 있고, 실사용 중 찾아내는 것이 목표입니다. 과장하지 않고 있는 그대로 안내합니다.

---

*문서 버전 기준일: 2026-07-15 · 이 가이드는 "지금까지 실제로 구현·검증된 기능" 기준으로 작성되었습니다.*
