---
description: SoDamAgentic 안전 기록 조회(F6). 사용자가 /sodam-agentic:log 로 실행하면, 지금까지 안전장치가 막거나 확인받은 내역을 쉬운 한국어로 요약해 보여준다.
---

# 소담 안전 기록 — 지금까지 뭘 막았나

사용자가 이 명령을 실행하면, 안전 기록 파일(`safety-log.jsonl`)을 Read 도구로 읽어 아래를 **쉬운 한국어로, 비개발자 기준**으로 요약하세요.

**파일 위치(2026-08-02부터, 두 곳 다 확인)**: 아래 순서로 둘 다 시도해 존재하는 파일을 읽으세요(둘 다 있으면 둘 다 읽어 시간순으로 합쳐서 보여주세요 — 위치가 바뀌면서 기록이 두 파일로 나뉠 수 있습니다).
1. `~/.claude/plugins/data/sodam-agentic/safety-log.jsonl` (새 위치, 공식 영구 데이터 경로가 실제로 적용됐을 때)
2. `~/.sodamagentic/safety-log.jsonl` (예전 위치, 여기만 있어도 정상 — 새 위치 적용 전 기록이거나 Codex 환경)

둘 다 없으면 "아직 기록 없음"으로 안내하세요.

## 읽는 법
- 파일은 JSONL(한 줄에 JSON 객체 하나)입니다. 각 줄: `{ id, action: "deny"|"ask", target, reason, created_at }`.
- `action`이 `deny`면 "막음", `ask`면 "확인 요청"으로 번역해서 보여주세요.
- `target`은 저장 전에 이미 비밀값이 `[REDACTED]`로 마스킹된 상태라 그대로 보여줘도 안전합니다.

## 보여줄 것
1. **최근 10건**(파일 맨 아래부터) — 날짜·시간, 막음/확인 여부, 무슨 이유였는지 한 줄씩.
2. **오늘 요약** — 오늘 날짜(`created_at` 앞 10자리, `YYYY-MM-DD`)로 필터링해 몇 건 막혔고 몇 건 확인받았는지.
3. 기록이 아예 없으면: "아직 안전장치가 막거나 확인받은 일이 없어요 — 안전하게 잘 쓰고 계신 거예요!"라고 안내.

## 정직 (한계)
- 이 기록은 **막히거나 확인받은 일만** 남습니다(안전하게 그냥 통과된 작업은 로그가 비대해지는 걸 막기 위해 기록 안 함). "오늘 AI가 한 일 전체" 기록이 아니라 **"안전장치가 개입한 일"** 기록입니다 — 과장하지 말고 정확히 안내하세요.

## 하지 말 것
- 원본 JSON을 그대로 덤프하지 마세요 — 항상 쉬운 한국어 문장으로 풀어서 설명하세요.
